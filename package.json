{
  "name": "tomekio-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page for Tomek.io",
  "scripts": {
    "clean": "rm -rf dist",
    "build:html": "node build.js",
    "build:css": "tailwindcss -i src/styles/tailwind.css -o dist/styles.css --minify",
    "build": "npm run clean && npm run build:html && npm run build:css",
    "dev": "npm run build:html && tailwindcss -i src/styles/tailwind.css -o dist/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
